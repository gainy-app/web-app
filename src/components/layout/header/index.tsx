import {Image} from 'components/common/Image';

export const Header = () => {
  return (
    <header>
      <nav>
        <Image type={'logo'}/>
        <div>link</div>
      </nav>
    </header>
  );
};
